{
  "name": "mentor",
  "version": "1.0.0",
  "description": "The Local Loom Logic Mentor (LLLM) – A private, local-first sanctuary for bicultural identity.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cp -r css dist/ && esbuild src/main.ts src/serviceWorker.ts --bundle --outdir=dist --minify --format=esm --external:url --external:path --external:fs",
    "watch": "cp -r css dist/ && esbuild src/main.ts src/serviceWorker.ts --bundle --outdir=dist --watch --format=esm --external:url --external:path --external:fs",
    "serve": "python -m http.server 8866 -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "^0.2.81"
  }
}
